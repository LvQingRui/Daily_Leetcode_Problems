这道题我们需要知道，时间复杂度如何去计算，这不同于其余的双循环，直接就是 $O(n^2)$， `left` 在整个程序中，也只会从 `0` 增加到
`n`,
left 永远不会回退，所以 `left += 1` 最多执行 `n` 次。

其实换个角度理解，如果这段代码的时间复杂度为 $O(n^2)$，就需要、满足:对于每一个`right`，`left`都是从头走到尾的。但是在我的代码里，
left是没有重置的。

一句话总结：
虽然代码中有 `while` 循环，但 `left` 指针在整个过程中只会单调递增一次，因此所有操作的总次数是线性的，时间复杂度为 $O(n)$。